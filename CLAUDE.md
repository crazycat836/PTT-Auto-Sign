# CLAUDE.md — PTTAutoSign

PTT BBS 每日自動簽到，結果透過 Telegram 回報。以 Docker 容器部署在
Synology DS923+（amd64）。

## Agent skills

### Issue tracker

Issues 追蹤在本 repo 的 GitHub Issues（crazycat836/PTT-Auto-Sign），
透過 `gh` CLI 操作。See `docs/agents/issue-tracker.md`.

### Triage labels

沿用五個預設標籤，名稱與角色同名。See `docs/agents/triage-labels.md`.

### Domain docs

Single-context：根目錄一份 `CONTEXT.md` 加 `docs/adr/`。
See `docs/agents/domain.md`.
